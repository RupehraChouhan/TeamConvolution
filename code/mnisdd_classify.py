def mnisdd_classify(x_test):
    # x_test is a 2D matrix of size [NUM_SAMPLES, 4096]
    # Each row of x_test is a 64x64 gray scale image that has been vectorized
    # where NUM_SAMPLES is the amount of image in the test set
    # In this final project, NUM_SAMPLES = 10000
    raise NotImplementedError
    predicted_labels = []
    # Create your classification result here
    # predicted_labels must be a 2D matrix of size [NUM_SAMPLES, 2]
    # Each row of predicted_labels must be in ascending order
    # For example, if digits 7 and 5 are in one image, the labels must be [5, 7]
    return predicted_labels
